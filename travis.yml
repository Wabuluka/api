#got this organised structure from github
# Config file for automatic testing at travis-ci.org
sudo: True  # http://docs.travis-ci.com/user/migrating-from-legacy/
language: python
python:
  - 3.6
install: pip install -r requirements/dev.txt
